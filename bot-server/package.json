{
  "name": "magicmerge-bot-server",
  "version": "0.0.1",
  "private": true,
  "description": "Cloudflare Worker — Telegram bot for Magic Merge: /start commands + scheduled push notifications",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251101.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.50.0"
  }
}
